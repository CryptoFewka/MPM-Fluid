#include "ofxMPMFoodPellet.h"

ofxMPMFoodPellet::ofxMPMFoodPellet(float inx, float iny, int inCharges, float inRadius) {
	x = inx;
	y = iny;
	charges = inCharges;
	radius = inRadius;
	active = true;
}
